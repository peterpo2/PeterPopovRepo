using Agency.Core;
using Agency.Core.Contracts;
using Agency.Models;
using Agency.Models.Contracts;
using System.Collections.Generic;

namespace Agency
{
    class Startup
    {
        static void Main()
        {
            /*IRepository repository = new Repository();
            ICommandFactory commandFactory = new CommandFactory(repository);
            IEngine engine = new Engine(commandFactory);
            engine.Start();
*/
            /*IBus bus = new Bus(1, 35, 1.5, true);
            IAirplane airplane = new Airplane(2, 40, 1.8, true);

            Vehicle v = new Bus(1, 35, 1.5, true);
            List<IVehicle> list = new List<IVehicle>();
            list.Add(v);
            list.Add(bus);
            list.Add(airplane);*/
        }
    }
}
